server:
  port: 8080

spring:
  application:
    name: todo-app
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: jai
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: admin
      password: admin

jwt:
  secret: jai
  expiration: 10